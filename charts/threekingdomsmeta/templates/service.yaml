apiVersion: v1
kind: Service
metadata:
  name: {{ include "threekingdomsmeta.fullname" . }}
  namespace: {{ include "threekingdomsmeta.namespace" . }}
  labels:
    {{- include "threekingdomsmeta.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "threekingdomsmeta.selectorLabels" . | nindent 4 }}
