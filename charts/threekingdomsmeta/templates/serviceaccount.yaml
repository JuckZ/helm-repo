{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "threekingdomsmeta.serviceAccountName" . }}
  namespace: {{ include "threekingdomsmeta.namespace" . }}
  labels:
    {{- include "threekingdomsmeta.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
