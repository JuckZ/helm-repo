apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "threekingdomsmeta.fullname" . }}-test-connection"
  labels:
    {{- include "threekingdomsmeta.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "threekingdomsmeta.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
